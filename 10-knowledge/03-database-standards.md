# 数据库规范

> 占位：MySQL 设计与变更约定。

## 设计
- 每表有主键；业务唯一约束显式声明
- 必要索引写明用途；避免盲目加索引
- 软删除/审计字段按项目统一（待定）

## 变更
- 一切结构变更进 `04-database/03-initialization-sql/` 或 `04-migration-scripts/`，并更新 `04-database/` 文档
- 破坏性变更必须附回滚方案
- 禁止在文档未更新的情况下只改库

## 待填
- [ ] 字符集/排序规则
- [ ] 字段类型选用表
- [ ] 分表/归档策略（如需要）
