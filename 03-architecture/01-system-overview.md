# 系统总体架构

> 输入：`02-product/01-product-requirements.md`；可用 `06-prompts/02-architect.md` 生成。

## 1. 架构目标
- 支撑范围：`[示例: 单体可演进]`
- 质量属性：可维护 / 可观测 / 可回滚

## 2. 逻辑视图（文字）

```
[客户端] → [API 网关/反向代理] → [应用服务] → [MySQL]
                              ↘ [Redis]（可选）
```

## 3. 物理 / 部署视图（摘要）
- 环境：dev / test / prod（详见 `06-deployment-architecture.md`）
- 默认端口：应用 8080，管理 9090

## 4. 关键设计决策（ADR 摘要）

| ID | 决策 | 状态 | 备注 |
|----|------|------|------|
| ADR-001 | `[示例: 先单体，预留模块边界]` | 提议 | |
| ADR-002 | 配置与密钥分离 | 已采纳 | 见部署规范 |

## 5. 上下文边界
| 外部系统 | 方向 | 协议 | 说明 |
|----------|------|------|------|
| `[示例: SSO]` | 入 | HTTP | 可选 |

## 验收
- [ ] 与 PRD 范围一致
- [ ] 图示可放入 `diagrams/`
